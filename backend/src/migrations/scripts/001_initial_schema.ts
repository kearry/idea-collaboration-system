// Example migration script
// migrations/scripts/001_initial_schema.ts
export const up = async () => {
    // This migration doesn't need to do anything as the schemas are already defined
    // through Mongoose models when the application starts
};
